project(test_$project)

include_directories(${test_include_path})

file(GLOB_RECURSE all_files
*.cpp
*.cc
*.c++
*.c
*.C)

link_directories(${test_link_path})

add_executable(test_$project $${all_files})

target_link_libraries(test_$project $project ${test_lib})